//:: IgnoreFile(/voila/issue/36/)

region R(id s, int x)
  guards { duplicable G; }
  interpretation { true }
  state { 0 }
  actions {}

procedure test(id s) returns (int ret)
  requires true;
  ensures R(s, ret);
{
  ret := 0;
  fold R(s, ret);
}
