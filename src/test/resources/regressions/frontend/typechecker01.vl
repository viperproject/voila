struct memcell {
  int f;
}

int test02(bool b) {
  //:: ExpectedOutput(typechecker_error)
  ret := b;
}

void test03(memcell x) {
  bool v;

  //:: ExpectedOutput(typechecker_error)
  v := x.f;
}

void test04()
  //:: ExpectedOutput(typechecker_error)
  requires 0;
{}
