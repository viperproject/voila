region R(id s)
  guards { unique G; }
  interpretation { true }
  state { 0 }
  actions {
    ?n | n == 0 | G: 0 ~> n;
    ?n | n == 1 | G: 1 ~> n;
  }

procedure test01(id s)
  requires R(s, ?b);
{
  set<int> bs;

  bs := Set(?c | c == b);
  bs := Set(?c | c == b);
}
