region R(id s)
  guards { unique RG; }
  interpretation { true }
  state { true }
  actions {}

procedure test01() {
  set<bool> bs;

  bs := Set[bool](?c | c);
}

procedure test02() {
  set<bool> bs;

  // bs := Set(?c | c); // TODO: Requires better type inference
}

procedure test03(id s)
  requires R(s, ?b);
{
  set<bool> bs;

  bs := Set[bool](?c | c == b);
}

procedure test04(id s)
  requires R(s, ?b);
{
  set<bool> bs;

  bs := Set(?c | c == b);
}



region RR(id s)
  guards { unique RRG; }
  interpretation { true }
  state { 0 }
  actions {}

procedure callee01(set<int> xs)

procedure test05(id s)
  requires RR(s, ?v);
{
  callee01(Set[int](?w | w <= v));
}

procedure test06(id s)
  requires RR(s, ?v);
{
  callee01(Set(?w | w <= v));
}
