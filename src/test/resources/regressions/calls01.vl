struct memcell {
  int f;
}

region Counter(id c, memcell x)
  guards { duplicable G; }
  interpretation {
    x.f |-> ?n
  }
  state { n }
  actions {
    G: ?m ~> Int;
  }

abstract_atomic int read(id c, memcell x)
  interference ?n in Int on c;
  requires Counter(c, x, n) && G@c;
  ensures Counter(c, x, n) && G@c;
  ensures ret == n;
{
  open_region
    using Counter(c, x);
  {
    ret := x.f;
  }
}

void test01(id c, memcell x)
  requires Counter(c, x, ?n) && G@c;
{
  int v;
  v := read(c, x);
  assert v == n;
}
