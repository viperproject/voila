===== Iterative =====
  --- Encoded ---
    BoundedCounter.t
      - TODO: makeCounter()
      - TODO: Voila issues #28 and #29
      - Encoded variants
    BoundedReadWriteLock.t
      - TODO: makeLock()
      - Caper's original symmetry of readers w.r.t. concrete state transitions unfortunately
        doesn't transfer to our encoding because we don't support non-deterministic choice
      - Encoded variants (particularly interesting: BoundedReadWriteLock-variants03.vl)
    CASCounter.t
      - TODO: makeCounter()
    ForkJoin.t
      - TODO: make_join()
    ForkJoinClient.t
      - TODO: make_join()
      - TODO: main() --- mostly manually encoded
    SpinLock.t
      - TODO: makeLock()

  --- Not yet encoded ---
    BagStack.t
      - TODO: Might need mathematical pairs or predicates with multiple out-arguments,
              e.g. either BagList(r, x, ?pair) and fst(pair)/snd(pair)
              or BagList(r, x, ?fst, ?snd)
    Barrier.t
      - TODO: Indexed guard algebra
      - TODO: Counting guard algebra
      - TODO: Sum guard algebra
    BarrierClient.t
      - TODO: Indexed guard algebra
      - TODO: Counting guard algebra
      - TODO: Sum guard algebra
    CounterClient.t
      - TODO: Counting guard algebra
    IncDec.t
      - TODO: Product guard algebra
    Queue.t
    ReadWriteLock.t
      - TODO: Counting guard algebra
    ReferenceCount.t
      - TODO: Counting guard algebra
    TicketLock.t
      - TODO: Indexed guard algebra
    TicketLockClient.t
      - TODO: Indexed guard algebra
